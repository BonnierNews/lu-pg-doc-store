version: '3'
services:
  postgres:
    image: postgres:9
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=et
      - POSTGRES_PASSWORD=ET
      - PGDATA=/tmp
    volumes:
      - ./dev-container/postgres:/docker-entrypoint-initdb.d/
